# 08 阴影系统

> 对比对象：HT 的场景级阴影，对照 Three.js r125 的逐灯光阴影。

## 8.1 共同原理

两边都使用 shadow map：

1. 从阴影方向渲染场景深度；
2. 把当前片元转换到阴影相机空间；
3. 采样深度纹理并比较当前深度；
4. 用 PCF 或软阴影过滤得到遮挡比例。

差异在于阴影资源的所有权和数量。HT 把阴影作为 Graph3dView 的场景级资源；Three.js r125 为每个启用 castShadow 的灯光维护独立的 ShadowMap。

## 8.2 HT 的场景级阴影

src/renderers/shadow-map.js 导出阴影管理器。它维护：

- 一个正交阴影相机的 view/projection 矩阵；
- 一张可调整尺寸的深度 RenderTarget；
- shadowDegreeX、shadowDegreeZ、shadowBias、shadowBlur 和 shadowQuality；
- 阴影是否启用、是否包含透明对象；
- 当前参与投射的 Data 包围盒。

阴影方向由场景样式控制，不依赖 DataModel 中某个 DirectionalLight 的位置。shadow-map.js 根据投射对象的包围盒调整正交相机范围，然后调用 src/graph3d/depth-renderer.js 的 renderDepth。

## 8.3 深度 pass

src/graph3d/depth-renderer.js 使用独立 depth 程序：

    renderDepth(renderTarget, includeTransparent,
                projectionMatrix, viewMatrix, width, height, drawRange)

它绑定目标纹理，清理深度/颜色缓冲，遍历 Graph3dRenderList，按对象的 shadow.cast 和 render layer 过滤，绘制模型的深度。src/renderers/shaders-postprocessing.js 提供 shadowMap 程序和 packDepthToRGBA。

HT 用 RGBA 打包深度，兼容没有深度纹理或需要统一采样格式的 WebGL 环境。正常材质 shader 再从 shadowMap 读取并解码。

## 8.4 HT 的采样和质量

src/renderers/shaders-library-features.js 中包含 getShadow、getShadowPCF、getShadowPCFSoft 等采样函数。HT 采用有限数量的邻域采样，质量由 shadow type、shadow blur 和纹理尺寸决定。场景级资源使所有物体共享同一阴影方向、偏置和分辨率。

阴影对象通常由 shadow.cast 和 shadow.receive 样式控制。透明物体是否进入深度 pass 由渲染列表和 includeTransparent 参数共同决定。

## 8.5 Three.js r125

Three.js 的 WebGLShadowMap 为每个 DirectionalLight、SpotLight 或 PointLight 创建 shadow camera、render target 和深度 pass。DirectionalLight/SpotLight 使用二维阴影贴图；PointLight 需要立方体六面渲染。

Three.js r125 支持 shadowMap.type 选择 Basic、PCF、PCFSoft 和 VSM 等路径，并在 Light.shadow 上配置 mapSize、bias、radius、camera near/far。阴影接收方由 material.lights 和对象可见性参与渲染。

## 8.6 架构对照

| 对比点 | HT | Three.js r125 |
|---|---|---|
| 阴影数量 | 场景级一套 | 每个灯光一套 |
| 主方向 | shadowDegreeX/Z | 灯光位置/方向 |
| 阴影相机 | 一个正交相机 | 每灯独立，点光源六面 |
| 深度 pass | DepthRenderer + 独立程序 | WebGLShadowMap |
| 深度格式 | RGBA 打包采样 | 深度纹理或 VSM |
| 质量参数 | 场景级 size/type/blur/bias | Light.shadow 级 mapSize/type/radius/bias |
| 对象控制 | shape3d.shadow.cast/receive | Object3D.castShadow/receiveShadow |
| 多光源阴影 | 不提供多套阴影 | 原生支持多个灯光 |

## 8.7 取舍

HT 的方案适合工业场景：一套稳定的方向光阴影，资源和调参简单，渲染列表容易统一批处理。缺点是不同灯光不能拥有不同阴影，也没有 Three.js 那样的点光源六面阴影和通用 VSM 路径。

Three.js 的方案更灵活，适合灯光复杂的通用 3D 场景；代价是每个阴影灯都会增加 pass、纹理和相机管理，应用还需要自己决定哪些业务对象应该投射或接收阴影。
