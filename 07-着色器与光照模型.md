# 07 着色器与光照模型

> 对比对象：HT 与 Three.js 的着色器体系；相关 shader 指纹对应 Three.js r133～r136。

## 7.1 当前 HT shader 组织

HT 的 shader 源码按职责放在 src/renderers/：

| 文件 | 内容 |
|---|---|
| shaders-library-core.js | common、packing、bsdfs、矩阵和灯光公共函数 |
| shaders-library-lighting.js | PBR/Phong 光照、AO、编码、tone mapping、cubeUV |
| shaders-library-features.js | 阴影、雾、裁剪、skinning、normal map、环境光 |
| shaders-materials.js | PBR、Phong、Lambert、pick、depth 和 blit 模板 |
| shaders-effects.js | FXAA、Bloom、景深、反射和流动效果 |
| shaders-postprocessing.js | 深度、环境贴图转换、模糊和全屏 pass |
| program-manager.js | define 注入、include 展开、循环展开和程序缓存 |

shader chunk 以 shaderTypeMap 中的字符串保存。程序模板通过 include 名称组合，program-manager 在编译前替换 include、宏和固定次数循环。

Three.js r125 对应 ShaderChunk、ShaderLib、WebGLProgram 和 examples/jsm/postprocessing。组织方式相似，但 Three.js 的 chunk 服务通用 Material；HT 的 chunk 还要服务对象级拾取、批处理、流动和 OIT。

## 7.2 PBR 同源指纹

| 指纹 | HT 当前实现 | Three.js 对照 | 判定 |
|---|---|---|---|
| DFGApprox 与多散射常量 0.047619 | shaders-library-lighting.js | r125 lights_physical_pars_fragment.glsl.js | 公式逐字一致 |
| computeSpecularOcclusion | shaders-library-lighting.js | r125 同名函数 | 公式逐字一致 |
| cubeUV maxMipLevel 8、tile 256/16 | shaders-library-lighting.js | r125 cube_uv_reflection_fragment.glsl.js | 布局一致 |
| PhysicalMaterial.specularF90 | shaders-library-lighting.js | r133+ | r125 结构不同 |
| 六参数 BRDF_GGX | shaders-library-lighting.js | r133+ | r125 为旧签名 |
| IBLSheenBRDF | shaders-library-lighting.js | r136 | r125 没有 |
| transmission vec4 和 attenuationColor | shaders-library-lighting.js | r132～r136 | r125 只有 stub |

综合指纹说明 HT 采用了 Three.js r133～r136 的相关 PBR shader 片段，而不是完整的 r125 shader。HT 进一步增加 fresnelIntensity、IOR、transmission、业务环境贴图和自己的 uniform 命名。

> 2026-09-22 复核：`BRDF_GGX` 的实际调用点是六参数形式
> `BRDF_GGX(lightDir, viewDir, normal, specularColor, specularF90, roughness)`（r125 为旧签名）；
> `fresnelIntensity` 的实现不在本文件，而在 `src/renderers/shaders-library-core.js`（本轮更正）。

## 7.3 PBR 光照路径

HT PBR 片元路径大致是：

    材质 uniform
      -> 法线/贴图/alpha
      -> 直接光 RE_Direct_Physical
      -> 间接漫反射和环境镜面
      -> clearcoat / sheen / transmission
      -> tone mapping
      -> 颜色编码与输出

shaders-library-lighting.js 中的 F_Schlick、BRDF_GGX、DFGApprox、computeMultiscattering、RE_Direct_Physical 和环境 BRDF 与 Three.js 的物理材质路径具有明显逐段对应关系。HT 的 F_Schlick 在结果上乘 fresnelIntensity，允许业务样式控制菲涅尔强度。

## 7.4 Phong、Lambert 和 headlight

HT 的 Phong 和 Lambert 程序仍使用相同的 lightsCommon、normal、fog、shadow 和 tone mapping chunk，但直接光、环境光和 headlight 由 Graph3dView 的场景状态填充。HT 没有 Three.js 的 PHYSICALLY_CORRECT_LIGHTS 宏，而是在 JavaScript 侧通过 headlightRatioForPhysicalMaterial 等配置调整物理光照比例。

Three.js r125 的 MeshPhongMaterial、MeshLambertMaterial 和 WebGLProgram 使用固定的材质宏和灯光数组。灯光颜色、强度和衰减由 WebGLPrograms/WebGLLights 生成 uniform。

## 7.5 环境、AO 与 transmission

HT 的 envmapPhysical chunk 只走 HT 的 cubeUV 预过滤环境贴图，镜面采样 textureCubeUV，漫反射采样环境法线。AO 通过 aoMap 和 computeSpecularOcclusion 影响间接镜面。

HT 的 transmission 路径包含折射射线、粗糙度到 mip、帧缓冲采样、Beer 衰减和 IBL 体积折射。Three.js r125 的 transmission 仍是很小的占位实现，完整 vec4 透射结构出现在后续版本。

## 7.6 shader 编译与缓存

src/renderers/program-manager.js 做三件关键工作：

1. 根据材质、灯光、阴影、贴图、OIT、flow 和 picking 状态生成 define；
2. 递归展开 include，并把固定上限的灯光、纹理或流动对象循环展开；
3. 以 define 位图和材质参数生成程序缓存键，复用已编译 WebGLProgram。

Three.js r125 的 WebGLProgram 也会 resolveIncludes、注入 prefix defines 和缓存程序，但不需要处理 HT 的 render layer、pickColor、batch instance、flow texture 数组等业务分支。

## 7.7 对比结论

HT 的 PBR 数学不是独立发明，而是对 Three.js shader 体系的移植、改名和扩展；HT 独有的部分集中在业务 uniform、headlight、对象拾取、flow、OIT、分面材质和 transmission 开关。Three.js 提供更通用的材质组合边界，HT 提供更完整的业务渲染路径。

