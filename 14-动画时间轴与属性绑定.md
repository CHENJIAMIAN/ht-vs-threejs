# 14 动画时间轴与属性绑定

> 对比对象：HT 的动画时间轴与属性绑定体系，对照 Three.js r125 的 Clip / Mixer / PropertyBinding。

## 14.1 工程里的两套动画，先分清

HT 工程里并存两套动画系统，服务的目标不同：

| | 业务动画（13 章） | 时间轴动画（本章） |
|---|---|---|
| 入口 | `ht.Default.startAnim({duration, easing, action})` | `ht.Animation` 命名空间 |
| 驱动对象 | 回调里手写 `node.p3()` / `node.s()` | 轨道按属性名自动写回目标 |
| 数据组织 | 无轨道概念，只有时间与回调 | Track / Clip / Timeline 三级结构 |
| 典型用途 | 一次性过渡、飞行、状态切换 | 可循环、可叠加、可混合、可嵌套的资产动画 |
| Three.js 对照 | 应用自己写的补间 + rAF | `AnimationClip` / `AnimationMixer` / `AnimationAction` |

判据很简单：需要「按时间点摆一串关键帧、还要循环/变速/叠加」时用本章这套；只是「A 状态过渡到 B 状态」用 13 章的 `startAnim` 更省事。

## 14.2 `ht.Animation` 的成员

`src/animation/animation-namespace.js` 组装出 `ht.Animation`，它本身是个普通对象，不是类：

| 成员 | 文件 | 职责 |
|---|---|---|
| `makeTrack` / `makeClip` | animation-namespace.js | 工厂：现造一条轨道 / 一份片段 |
| `play` / `playMono` / `stop` | animation-namespace.js | 把片段绑到目标上播放一次；单轨道简写；停掉某个动画实例或按 id 停 |
| `Track` | animation-track.js | 一条属性随时间变化的曲线 |
| `Clip` | animation-clip.js | 多轨道打包，带整体缓动与时长 |
| `Timeline` | animation-timeline.js | 把若干子动画按时间排布成时间线 |
| `Timer` | animation-timer.js | 带时间轴与事件的播放器，Timeline / TargetedAnimation 的基类 |
| `TargetedAnimation` | animation-targeted-animation.js | 把 Clip 绑到某个目标对象上播放（Timer 子类） |
| `Binding` | animation-binding.js | 值的累加器（**不是** `ht.Binding`，见 14.7） |
| `Runtime` | animation-runtime.js | 全局唯一的调度引擎 |
| `Util` | animation-util.js | 插值工具集，Track 的底层 |
| `LoopMode` | animation-loop-mode.js | `Once` / `Repeat` / `PingPong`（`Default` 别名等于 `Once`） |

另有 `ht.Binding`（`src/animation/binding.js`）单独挂在 `ht` 下，是属性标识→访问器的规则表，见 14.7。

## 14.3 Track：一条曲线

一条轨道 = **一列时间点 + 每个时间点的值 + 每段的缓动**，三者按下标一一对齐。

```js
const track = ht.Animation.makeTrack("position", [0, 1000], [[0, 0, 0], [10, 0, 4]]);
```

- **值形态由步长（stride）决定**：1 是标量、3 是三维向量、4 是四元数或欧拉角；插值方式随之切换。
- 名字**整体等于** `rotation` 或 `euler`（正则 `/^(rotation|euler)$/i`）且步长 ≥3 时，推断为欧拉角轨道；`localRotation`、`rotate` 这类包含匹配不算。
- 关键帧操作：`add` / `removeKeyAtTime` / `clearKeys` / `sortKeyFrames` / `getMaxTime` / `trim` / `shift` / `scale` / `scaleTo` / `clone`。
- 端点求值结果有缓存，且缓存存的是深拷贝副本——调用方拿到的数组不会和缓存共享内存。
- `setAdditive` 打开叠加模式后，结果会再乘上反向参考四元数。

缓动可以传函数，也可以传名字（交给 `ht.Default.createEasing` 解析）。

## 14.4 Clip 与 Timeline

**Clip** 是一组轨道：自己不产生值，只负责把每条轨道在同一时刻的结果按轨道名交给对应接收方。可以设一个整体缓动，设了之后 `evaluate` 先把时间按缓动曲线压一下再分发。`duration` 传负数（默认 `-1`）时取所有轨道里最晚的时间点。

**Timeline** 是时间线的嵌套编排，难点在「某一帧里谁该被求值」：

- 子项按 `pos` 排布；每帧要么落在子项区间内（按比例换算局部时间），要么刚跨过它的两端（补端点）；
- 正向/反向、循环轮次变化会决定「从哪个时间开始算跨过」，这是回调不重不漏的关键；
- `add(child, childStart, childDuration, timelinePosition, timelineDuration)` 支持用模式串写时间：普通数字、**百分数**（相对时间线总时长）、`=`（接着上一个子项的位置）、`>`（接着上一个子项的结束位置）、以及数字后加 `s`；
- `callAt(time, callback, once)` 注册的回调按同样的跨过判断触发；还有 `callAtOnce`、`removeCallAt`、`removeCallBetween`、`removeCallAtTime`、`clearCalls`；
- `scrub(startTime, endTime)` 可用于把一段区间快速走一遍。

## 14.5 Timer 与 TargetedAnimation

**Timer** 管参数、访问器与事件，**不自己推进时间**：

- 参数：`fps`（默认 60）、`frames` 优先于 `duration`（给了帧数就按帧数算时长，两者都没给默认 1 秒）、`loop`、`loopMax`、`delay`、`loopDelay`、`speed`、`autoPlay`、`stopWhenFinished`；
- 事件：`start` / `stop` / `pause` / `resume` / `loop` / `update` / `finished`，既能用 `onStart` 这类属性式回调，也能 `on(key, listener)` / `once` / `off` / `clearEvents`；
- 操作：`play(speed, time, loop, delay)`、`start`、`stop`、`pause`、`resume`、`seek`、`setTime`、`getTime`、`setSpeed`、`forwardTime`、`backwardTime`、`evaluate`、`flush`。

**TargetedAnimation** 是「Clip + 目标对象」：它把「目标 + 轨道名」注册成值累加器，每帧把各轨道的求值结果分发下去，并支持独占播放时的顶替（`setExclusive`）、`weight`、`enabled`、`shiftTime`，以及 `setAnimationPresets({speed, shiftTime, loop})` 一次性设常用参数。

## 14.6 Runtime：唯一的调度引擎

`Timer` / `Timeline` / `TargetedAnimation` 三个类的方法体几乎**全部转发**给 `Runtime`，所以它是这套系统里真正干活的那个：

1. **活动动画表**（id → 动画信息）：每次 `play` 登记一条；
2. **帧驱动**：向 `ht.Default.registerHeartbeat` 注册一次全局心跳（只注册一次，之后一直挂着），逐帧推进每条动画的时间、按循环模式折算显示时间、派发事件；
3. **绑定表**（目标 → 轨道名 → 值累加器）：每帧轨道求值后先把值攒进缓冲区，**帧末统一 `resolve()` 归并一次**再写回目标；
4. **待派发事件队列**：按 (动画, 事件名) 成对存放，避免每帧新建对象；每 1000 帧清一次队列兜住极端情况的积压；
5. **淡入淡出池**：单轨道与双轨道各一个空闲池，复用淡入淡出用的临时动画对象。

`stopAll` / `stop` / `pause` / `resume` / `isPlaying` / `isRunning` / `isPaused` / `getAnimation` / `getAnimationMap` / `getAnimationInfo` 都在这里。

「帧末统一归并」是这套设计的要点：同一帧里十条轨道改同一个目标，不会写十次中间态，只写一次最终值。

## 14.7 `ht.Binding`：名字到访问器的规则表

`ht.Binding`（`src/animation/binding.js`）把「一个属性标识」解析成一对 getter/setter，供动画按名字读写属性。`findFinalRule` 的尝试顺序：

1. 标识本身就是一个规则（函数，或带 `setter` / `func` 的对象）；
2. 标识命中注册表（`setBindingRule` 注册的字面名）；
3. 标识是字符串且命中某条 pattern 规则（`setBindingRulePattern` 注册的正则）；
4. 都没命中且有 `owner` 时，退回两条内置规则：owner 上有同名方法则走「方法调用」语义，否则走「字段读写」语义。

两个实用前缀：

- `func@name`：走方法访问语义——目标上同名成员是函数就直接调用，否则退化成 `getName()` / `setName()`。键名只在首次调用时解析一次，getter 与 setter 共用这次结果。
- `style@opacity`：读写样式字段。同类还有 `funcFlatten@position` 这种带平坦化处理的写法。

`setTempBindingRule(rule)` 给「函数 / 规则对象」自动生成一个字面规则名，动画域内部用它把匿名规则表进命名空间。

**别和 `ht.Animation.Binding` 搞混**：`ht.Binding` 是规则注册表（名字 → 访问器），`ht.Animation.Binding` 是值的累加器（把一帧内的多个值攒起来归并后写回目标）。源代码里两者都在 `src/animation/` 下，但职责完全相反。

## 14.8 状态绑定：`ht.Default.makeStateFul`

`src/animation/state-binding.js` 提供 `makeStateFul`，它给类挂上「状态」能力（`getState` / `setState` / `snapState`）：实例首次访问状态时，按规则表逐键解析成 getter/setter，用 `Object.defineProperty` 挂到实例的 `_state` 对象上。

它只在三个类上调用（必须最后求值，要等三个类都注册完成）：

| 类 | 规则表 | 状态键 |
|---|---|---|
| `Node` | `ht.Default.NodeStateBindings` | `localPosition3d, scale, localEuler` |
| `GraphView` | `ht.Default.GraphViewStateBindings` | `zoom, tx, ty` |
| `Graph3dView` | `ht.Default.Graph3dViewStateBindings` | `eye, center, up, orthoWidth, fovy, near, far, fog, fogDensity, fogColor, fogNear, fogFar, headlight* 等 |

**坑**：`_state` 上的访问器刻意不设 `enumerable`（与官方一致），所以 `Object.keys(node.getState())` 是空数组、`JSON.stringify` 也看不到内容。想枚举状态键要查规则表，别指望反射。

## 14.9 缓动

`ht.Math.Easing`（`src/math/easing.js`）是缓动族，`ht.Default.createEasing(name)` 是名字工厂（带缓存，同名同函数）。每个缓动是一个类，基类 `Easing` 按 `Easing.Mode`（In / Out / InOut）把子类的 `easeImpl` 派生成三种形态，子类只提供实现与构造参数默认值。参数按位置传入，缺省用定义里的默认值，例如 `new Easing.Bezier(0.2, 0.6)` 只覆盖 `x1` / `y1`。

可用名字：Linear、Quadratic、Cubic、Quartic、Power、Sine、Exponential、Circle、Elastic、Back、Bounce、Bezier，以及 Expo / Pow / Quad / Quart / Sin 五个别名。

## 14.10 与 Three.js r125 的逐项对照

| 概念 | HT | Three.js r125 |
|---|---|---|
| 单条曲线 | `ht.Animation.Track`（times + values + easings） | `KeyframeTrack`（times + values + interpolation 常量） |
| 片段 | `ht.Animation.Clip` | `AnimationClip` |
| 播放器 | `Timer`（基类）/ `TargetedAnimation`（绑目标） | `AnimationAction`（由 Mixer 创建） |
| 调度器 | `ht.Animation.Runtime`（全局单例，自注册心跳） | `AnimationMixer`（每场景/每根节点一个实例） |
| 时间推进 | 心跳自动逐帧推进，业务不用管 | 应用必须自己调 `mixer.update(delta)` |
| 属性寻址 | `ht.Binding` 规则表：`func@x`、`style@opacity`、pattern 注册 | `PropertyBinding`：解析 `nodeName.property[accessor]` 形式的轨道名 |
| 循环模式 | `LoopMode.Once` / `Repeat` / `PingPong` | `LoopOnce` / `LoopRepeat` / `LoopPingPong` |
| 缓动 | 内建缓动族，每段关键帧可指定 | 无内建缓动；`interpolation` 只有 Discrete / Linear / SmoothStep 等插值方式 |
| 嵌套编排 | `Timeline` + 模式串（`=` `>` `%` `s`）+ `callAt` 回调 | 需要应用自己用多个 Action 与 `time` / `clip` 编排 |
| 叠加/混合 | `setAdditive`、`weight`、独占播放、淡入淡出池 | `additive` clip、`weight`、`crossFade`、`fadeIn/fadeOut` |
| 默认旋转顺序 | 动画域 `YZX`（核心 `Euler` 是 `ZYX`） | `XYZ` |
| 写回时机 | 帧末统一归并（同目标多轨道只写一次） | 每次 `mixer.update(delta)` 内按绑定顺序写 |

一句话概括：**结构与概念高度对应，控制权归属相反**。Three.js 把「什么时候推进」交给应用（`mixer.update`），HT 把这件事收进核心心跳；Three.js 把「属性怎么寻址」写成字符串正则解析，HT 把它做成一张可注册的规则表。

## 14.11 使用与迁移注意

1. **先确认轨道名的语义**：HT 的轨道名会走 `ht.Binding` 规则表，`func@x` 与裸 `x` 是两种不同寻址；Three.js 的轨道名是 `节点.属性` 语法，两者不能互抄。
2. **旋转顺序**：动画域默认 `YZX`，核心 `Euler` 默认 `ZYX`，Three.js 默认 `XYZ`。跨库搬旋转轨道必须先统一顺序，否则表现为「角度对但方向歪」。
3. **别反射读状态**：`getState()` 的键不可枚举，枚举出来是空的。
4. **两套 Binding 别混用**：`ht.Binding` 是规则表，`ht.Animation.Binding` 是值累加器。
5. **心跳是全局的**：`Runtime` 的心跳注册一次长期挂着；如果发现动画帧在空跑，先查活动动画表是不是没清干净。