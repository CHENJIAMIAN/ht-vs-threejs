# 13 动画与业务特性

> 对比对象：HT 的业务动画与数据绑定，对照 Three.js r125 `src/animation/`。

## 13.1 动画定位不同

HT 的动画首先服务业务状态：改变 Data 的位置、样式、属性和相机，触发 2D/3D 视图更新。Three.js 的 AnimationClip/Mixer/Action 首先服务资产动画：按轨道采样并写回 Object3D、骨骼、材质或 morph target。

两边都在时间轴上采样插值，但动画对象的归属不同：HT 动画属于 DataModel/视图，Three.js 动画属于 Object3D 和 AnimationMixer。

## 13.2 HT startAnim

src/core/core-json-browser-utilities.js 向 defaultConfig 注册 startAnim。它支持 duration 模式和 frames + interval 模式，回调接收 easing(t) 和线性进度 t：

    const animation = ht.Default.startAnim({
      duration: 800,
      easing: "easeInOut",
      action: (value, t) => {
        node.p3(10 * t, 0, 0);
        node.s("shape3d.opacity", 1 - 0.5 * t);
      }
    });

动画对象提供 tick、pause、resume、stop、isPaused、isRunning 和 finishFunc。callLater/requestAnimationFrame 负责调度；delay、finish 和暂停时的时间补偿由 startAnim 统一处理。

src/widget/widget-form-controls.js 的 flyTo、src/graph3d/globe-interactor.js 的惯性和 src/widget/createAnimationManagerMethods.js 的模型动画管理都复用这套调度思想。

## 13.3 HT 数据绑定

HT 的业务特性来自 Data 属性和样式绑定。src/graph/resolve-data-binding-value.js、src/graph3d/data3d-ui.js 和 PrefabRenderer 在渲染阶段解析：

- attr(name)：从 Data 业务属性取值；
- s(name)：从 Data 样式取值；
- 组合路径和转换函数：把温度、状态、功率映射成颜色、透明度、尺寸或模型；
- 绑定变化后的失效通知：视图下一次绘制读取新值。

例如，储能场景可以让 node.a("temperature", value) 驱动 shape3d.color 或热力图透明度，无需业务代码手动查找对应 Mesh。

## 13.4 3D 流动效果

src/graph3d/flow-renderer.js 读取 effect.flow、gradient、uvshape、size 和 intensity，维护 flow texture、flow matrix 和时间偏移；src/renderers/shaders-effects.js 和 shaders-materials.js 在 shader 中滚动 UV 并合成颜色。

流动效果适合管道方向、能量输送和状态指示。它是 HT 的材质/业务样式能力，不是通用关键帧资产动画。

Three.js 没有核心 flow renderer。应用通常写 ShaderMaterial uniform 时间、修改 UV，或使用后处理/纹理动画实现同样效果。

## 13.5 模型动画和插件

HT 的 widget-list-accordion-views.js、createAnimationManagerMethods.js 和 getAnimationRef.js 管理按轨道或帧的模型动画，支持播放、暂停、速度、循环和关键帧读取。动画结果最终写入 HT 模型矩阵或 Data 样式。

plugins/ht-animationplayer.js（若案例加载）和 plugins/ht-thermodynamic.js 是业务插件，不是核心 Graph3dView。热力图插件把温度点绘制成渐变纹理，再作为自定义 shape3d 模型的材质输入；Three.js 需要自行实现数据采样、纹理更新和材质绑定。

## 13.6 选中、高亮和状态动画

src/graph/selection-model.js 保存选择集合，GraphView 用 select.width/select.color 绘制描边；src/graph3d/highlight-helper.js 和相关 shader 处理 3D 的 outline、glow 和 pick 状态。选择本身是 DataModel 状态，所以 2D、3D、TreeView 和 PropertyView 可以同步响应。

Three.js 没有选择模型。常见做法是给选中 Mesh 改材质 emissive/color、使用 OutlinePass，或把业务选择存进 userData/外部 Map。

## 13.7 Three.js AnimationMixer

Three.js r125 的动画链是：

    AnimationClip
      -> KeyframeTrack
      -> AnimationMixer
      -> AnimationAction
      -> Object3D/material/bone 属性

Mixer.update(delta) 采样轨道并写回绑定属性。AnimationAction 支持 play、stop、pause、fade、crossFade、warp、weight、timeScale 和 LoopOnce/LoopRepeat/LoopPingPong。该系统适合 glTF/FBX 的骨骼、蒙皮和 morph 动画，但不理解 HT 的 DataModel、样式回退、业务属性或拓扑关系。

## 13.8 能力对照

| 对比点 | HT | Three.js r125 |
|---|---|---|
| 基本动画 | startAnim 回调改 Data 属性/样式 | Clip/Track/Mixer 采样对象属性 |
| 调度 | callLater + requestAnimationFrame | 应用调用 mixer.update(delta) |
| 业务绑定 | attr()/s() 直接映射渲染值 | 自己监听数据并写 uniform/对象 |
| 3D 流动 | effect.flow + FlowRenderer 内建 | ShaderMaterial/纹理动画自研 |
| 飞行 | flyTo、包围盒取景和插值内建 | Tween/Controls 自行组合 |
| 选中高亮 | SelectionModel + 样式/outline | 无核心概念，需自研 |
| 热力图 | 插件闭环：数据→纹理→模型 | 需自己生成纹理/顶点色和材质 |
| 资产动画 | 支持帧/轨道管理，结果写 HT 模型 | 骨骼、morph、轨道混合更完整 |

## 13.9 与 14 章的分工

13 章讲的是「业务动画」：用 `startAnim` 改 Data 的属性与样式，画面跟着变。

工程里还有第二套动画系统——`ht.Animation`（Clip / Track / Timeline / Timer / TargetedAnimation / Binding / Runtime）与配套的 `ht.Binding` 规则表、状态绑定、缓动族。它更接近 Three.js 的 Clip/Mixer 体系，也是两边唯一真正「同类可比」的动画子系统，单独放在 14 章。阅读动画相关内容时先确认自己要看的是哪一套。

## 13.10 结论

Three.js 的动画系统更适合播放资产时间轴；HT 的动画系统更适合“业务值变化后画面自动跟着变”。如果用 Three.js 实现 HT 的业务体验，至少还要补数据绑定、选择模型、流动 shader、热力图、飞行控制和统一调度。

