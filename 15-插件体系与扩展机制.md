# 15 插件体系与扩展机制

> 对比对象：HT 的插件与扩展机制，对照 Three.js r125 核心 + `examples/jsm`。

## 15.1 两种扩展哲学

| | HT | Three.js |
|---|---|---|
| 扩展单位 | 一个独立 `<script>` 文件（`ht-*.js`） | 一个 ES module（核心或 `examples/jsm`） |
| 接入方式 | 加载后往全局命名空间挂东西 | `import` 进来的类，由应用自己组合 |
| 是否有全局注册表 | **有**（见 15.2） | **没有**：扩展就是新类，只有 `Loader` 基类等少量约定 |
| 版本耦合 | 插件与核心版本强绑定 | 松耦合，按 semver 组合 |
| 典型扩展点 | 自定义 shape3d 模型、图片资源、组件类型、轨道类型、后处理模块 | Loader、Control、Pass、Material、几何生成器 |

这条差异的后果很实际：HT 的插件**改不了核心的代码结构**，只能往注册表里加东西；Three.js 的扩展是纯代码组合，组合出问题也只能由应用自己负责。

## 15.2 HT 的全局注册表

HT 的扩展几乎都收敛到 `ht.Default` 上的一组注册/反注册方法：

| 注册点 | 作用 |
|---|---|
| `setShape3dModel` / `getShape3dModel` | 注册自定义 3D 模型（名称 → 几何模型），供 `shape3d` 样式按名引用 |
| `setImage` / `getImage` | 注册图片资源（名称 → 图片），供样式与图标按名引用 |
| `setCompType` / `getCompType` | 注册 2D 组件类型 |
| `setTrackType` / `getTrackType` | 注册动画轨道类型 |
| `registerHeartbeat` / `unregisterHeartbeat` | 注册逐帧心跳回调 |
| `registerEasing` / `createEasing` | 注册与获取缓动 |
| `def(...)` | 类注册：把方法表、静态元数据与原型一次性挂好 |

特征是**按名字查表**：业务代码只写名字（如 `shape3d: "myModel"`），由注册表在渲染时解析。好处是工程 JSON 可序列化、跨版本稳定；代价是注册有顺序依赖——「加载即注册」的插件必须在核心之后加载。

Three.js 没有这一层：`new MyGeometry()` 直接就是一个对象，没有名字、没有注册表、也就没有顺序问题。需要用名字索引时，应用自己在 Map 里维护。

## 15.3 官方插件的形态

HT 的官方插件都是独立脚本，按功能分文件，常见的包括：

| 方向 | 插件 |
|---|---|
| 模型加载 | ht-obj、ht-fbx、ht-modeling、ht-bim、ht-splatting |
| 矢量与图形 | ht-vector、ht-form、ht-edgetype、ht-flow、ht-dashflow |
| 动画 | ht-animation、ht-animationplayer、ht-cssanimation、ht-sync |
| 渲染增强 | ht-postprocessing、ht-water、ht-wave、ht-cloud、ht-cloudrender、ht-sdf、ht-textureLoader |
| 交互与其它 | ht-dialog、ht-panel、ht-physicsEngine、ht-vr、ht-bvh、ht-thermodynamic、ht-editinteractor3d |

它们的共同特征：

1. **加载即注册**——脚本执行时把类或资源挂到 `ht` 命名空间或注册表；
2. **不改核心**——只新增能力，不替换核心类；
3. **与核心版本绑定**——插件按核心版本发布，混用不同版本的行为没有保证。

对照 Three.js：等价物是 `examples/jsm` 下的 Loader / Controls / PostProcessing 等目录，每个都是独立模块，`import` 后由应用组合。区别在于 Three.js 的 examples 不注册到全局，也不保证跨版本稳定。

## 15.4 与 Three.js 扩展机制的对照

| 对比点 | HT | Three.js r125 |
|---|---|---|
| 分发 | 单个 js，`<script>` 引入 | npm 包 + `examples/jsm` |
| 命名冲突 | 全局命名空间，靠插件自带前缀规避 | 模块作用域，天然隔离 |
| 加载顺序 | 加载即注册，顺序敏感 | import 即得，顺序无关 |
| 按名字索引 | 内建（注册表） | 需应用自建 Map |
| 类型信息 | 靠 API 文档与注解 | 自带类型声明 |
| 调试 | 出问题先查「注册上了没有」 | 出问题先查「组合对不对」 |

## 15.5 迁移提示

1. **别指望有全局注册表**：Three.js 里没有 `setShape3dModel` 的对应物，等价做法是自己写一个「名称 → 几何/材质工厂」的 Map，在应用启动时填好。
2. **插件的调用是「名字」而不是「对象」**：把 HT 业务代码搬到 Three.js 时，所有 `ht.Default.xxx(name)` 都要换成显式对象引用。
3. **顺序依赖要显式化**：HT 靠脚本顺序保证「先核心后插件」，改成 ESM 后这层顺序保证消失，需要在初始化代码里写清楚依赖。