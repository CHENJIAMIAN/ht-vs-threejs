# 11 相机交互与拾取

> 对比对象：HT 的相机与交互器，对照 Three.js r125 及其 examples 控件。

## 11.1 相机模型

HT 的 Camera 位于 src/graph/camera.js，是一个可以加入 DataModel 的 Node。Graph3dView 同时维护 eye、center、up、投影类型、near、far、fovy、aspect 和正交宽度。

Camera 的样式和方法支持：

- perspective/orthographic 切换；
- p3/r3/s3 变换；
- attachRenderTarget/detachRenderTarget 离屏相机；
- culling mask 和 render layer；
- lookAt、投影矩阵和视图矩阵；
- skybox、main、top、infinity 等相机优先级。

Three.js r125 将相机作为独立 Object3D：PerspectiveCamera 和 OrthographicCamera 分别保存 fov/aspect/near/far 或 left/right/top/bottom/near/far，position/quaternion/up 由 Object3D 提供，updateProjectionMatrix() 重新计算投影。

## 11.2 HT 的交互器

src/graph3d/ 下的交互器按用途拆分：

| 交互器 | 文件 | 能力 |
|---|---|---|
| DefaultInteractor | default-interactor.js | 旋转、平移、缩放和鼠标拾取 |
| MapInteractor | map-interactor.js | 地图平移、旋转和约束 |
| GlobeInteractor | globe-interactor.js | 球面轨道、惯性和飞行 |
| MoveInteractor | move-interactor.js | 图元移动和吸附 |
| EditInteractor | edit-interactor.js | 编辑控制柄和变换 |
| TouchInteractor | touch-interactor.js | 触摸拖动、双指缩放 |
| ScrollBarInteractor | scroll-bar-interactor.js | 滚动和视口联动 |
| TextEditInteractor | text-edit-interactor.js | 文字编辑 |
| 阻尼（惯性）方法集 | interactor-damping.js | `_enableDamping` / `_dampingFactor`，把位移累加后逐帧衰减施加 |

阻尼不单独成类，而是抽成 `interactorDampingMethods` 注入 `DefaultInteractor` 与 `MapInteractor`（不放在基类 `Interactor` 上）。默认关闭：`_enableDamping = false` 时走原有的「立刻施加」路径。

Graph3dView 通过 setInteractors() 组合交互器。旋转、缩放、平移、first person、边界限制、reset 和 flyTo 都是视图级能力。

Three.js r125 的 OrbitControls、FlyControls、TrackballControls 等位于 examples/jsm/controls，不属于核心 Renderer。它们需要应用实例化、监听 DOM 事件，并在开启阻尼时每帧调用 update()。

## 11.3 HT 的 GPU 拾取

HT 的拾取路径由 src/graph3d/pick-color-renderer.js、src/graph3d/graph3d-render-list.js 和 Graph3dView 的 readPixels 组成：

1. 为可拾取 Data 分配稳定的 pickColor；
2. 使用 pick shader 把对象或 face part 渲染到离屏 RenderTarget；
3. 根据鼠标位置调用 gl.readPixels()；
4. 用颜色查回 pickColorMap 中的 Data、UI 和 part；
5. 触发 hover、click、doubleClick 或编辑操作。

pickerResolution 可以低于屏幕分辨率，减少拾取 pass 成本。批处理模型会把 pickColor 写入实例颜色流，仍然可以反查到具体业务对象。

## 11.4 HT 的 CPU 辅助射线

src/graph3d/raycaster.js 提供几何射线、包围盒和三角形相交工具，编辑器用它计算控制柄、平面交点和移动位置；它不取代 Graph3dView 的对象级 GPU 拾取。

## 11.5 Three.js 的 Raycaster

Three.js r125 的 Raycaster 位于 three-0.125.0/package/src/core/Raycaster.js。典型流程是：

    raycaster.setFromCamera(pointer, camera);
    const hits = raycaster.intersectObjects(scene.children, true);

Raycaster 遍历 Object3D，调用 Mesh、Line、Points 等对象的 raycast()，最终对包围盒、三角形或线段做 CPU 求交。命中结果包含 distance、point、face、object 和 uv。

Three.js 没有核心 GPU pick pass。大场景需要应用自行渲染颜色 ID、建立 BVH、过滤 layers 或缓存可拾取对象。

## 11.6 相机动画

HT 的 widget-form-controls.js 提供 flyTo：根据目标 Data 包围盒计算视距和 center，再用 defaultConfig.startAnim 插值 eye/center/up。交互器也复用同一动画调度器实现惯性和回弹。

Three.js 没有内建的 flyTo。应用需要使用 Tween、GSAP 或自己插值 camera.position、quaternion 和 target，并在每帧更新 controls。

## 11.7 对比结论

| 对比点 | HT | Three.js r125 |
|---|---|---|
| 相机载体 | DataModel 中的 Camera/Graph3dView 状态 | PerspectiveCamera/OrthographicCamera 对象 |
| 交互器 | 视图内建并可组合 | examples 控件，应用手动接入 |
| 拾取 | GPU 颜色 pass，返回业务 Data | CPU Raycaster，返回 Object3D |
| 大场景 | 拾取成本主要与离屏分辨率有关 | 求交遍历与几何复杂度影响明显 |
| 飞行 | flyTo、包围盒取景、动画内建 | 需自己编写或引入动画库 |
| 编辑 | 移动/旋转/控制柄与 Data 属性联动 | 需要 TransformControls 和业务映射 |

