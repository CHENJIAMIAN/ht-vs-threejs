# 06 材质系统与样式体系

> 对比对象：HT 的样式驱动材质体系，对照 Three.js r125 的 Material 对象。

## 6.1 两种材质模型

HT 没有要求业务代码创建一个独立的 Material 实例。Node/Edge 通过 s() 写入 shape3d.* 样式，PrefabRenderer 和材质模块在生成渲染项时把样式翻译成 uniform、贴图和 WebGL 状态。

Three.js 把 Material 当作一等对象。MeshStandardMaterial、MeshPhysicalMaterial、MeshPhongMaterial 等实例保存颜色、粗糙度、金属度、透明、贴图和开关，Mesh.material 直接引用它。

HT 的样式更适合工程 JSON 和业务绑定；Three.js 的对象更适合代码复用和材质实例共享。

## 6.2 HT 的样式到 uniform

src/graph3d/graph3d-highlight-prefabs.js 注册 shape3d 和灯光的默认样式，src/renderers/material-maps.js、runtime-material-shading.js 和 src/graph3d/apply-material-render-uniforms.js 把样式映射到 shader：

    node.s({
      "shape3d": "box",
      "shape3d.color": "#4f46e5",
      "shape3d.roughness": 0.45,
      "shape3d.metalness": 0.2,
      "shape3d.opacity": 0.9,
      "shape3d.image": "metal.png"
    });

材质类型通常由 shape3d.material 决定，常见路径是 pbr、phong、lambert、basic 和 pick/depth。分面样式可以用 shape3d.top、bottom、from、to 的 color/image/material 覆盖整物体材质。

样式值可由业务属性绑定。Node 的 a("temperature") 改变后，resolveDataBindingValue 解析出新的颜色、透明度或模型参数，下一次渲染自动上传新 uniform。

## 6.3 PBR 参数对应

| HT 样式 | Three.js r125 材质字段 | 说明 |
|---|---|---|
| shape3d.color / diffuse | color | 基础色 |
| shape3d.emissive | emissive | 自发光颜色 |
| shape3d.roughness | roughness | 粗糙度 |
| shape3d.metalness | metalness | 金属度 |
| shape3d.opacity | opacity | 透明度 |
| shape3d.alphaTest | alphaTest | 透明裁剪阈值 |
| shape3d.image | map | 漫反射贴图 |
| shape3d.normal.image | normalMap | 法线贴图 |
| shape3d.roughness.image | roughnessMap | 粗糙度贴图 |
| shape3d.metalness.image | metalnessMap | 金属度贴图 |
| envmap / envmap.probe | envMap | 环境贴图来源 |
| shape3d.clearcoat.* | clearcoat* | 清漆层 |
| shape3d.sheen.* | sheen* | 丝绒层 |

HT 还提供 fresnelIntensity、transmissionEnabled、effect.flow、batchBrightness 和分面子材质等业务扩展；Three.js r125 的对应能力要靠 MeshPhysicalMaterial、ShaderMaterial 或自定义 pass 组合。

## 6.4 贴图和颜色空间

src/graph3d/apply-texture-uniforms.js、src/graph3d/view-textures.js 和 src/renderers/runtime-material-shading.js 管理纹理上传、贴图单元、采样参数、sRGB 转换和空纹理。多个分面和效果贴图会按渲染项的固定顺序分配纹理单元。

Three.js r125 由 WebGLTextures 和 WebGLMaterials 负责纹理上传、颜色空间和 sampler uniform。两边都有 map/normal/ao/emissive 等贴图概念，但 HT 的贴图来源可以直接写在工程样式中，Three.js 通常要先创建 Texture，再赋给材质字段。

## 6.5 光照和 headlight

HT 的 Light 是 DataModel 中的图元，位于 src/graph3d/light.js。灯光颜色、类型、强度、范围、角度、mask 和是否参与阴影都可以通过样式设置。Graph3dView 还维护 headlight：它根据相机或阴影方向合成场景主光，并可设置环境强度和物理材质比例。

Three.js 使用 PointLight、SpotLight、DirectionalLight、RectAreaLight 等独立对象。每个灯可以有颜色、强度、距离、角度和阴影相机，但没有 HT 的 style mask 通道；灯光选择通常通过 scene 层级、visible 或材质自定义 shader 完成。

## 6.6 选择、发光和后处理样式

HT 的 select.width/select.color、highlight.mode/highlight.type、shape3d.glow、effect.flow 和 bloom 样式直接参与渲染模型生成。src/graph3d/highlight-helper.js 和 src/graph3d/postprocessing.js 负责选中描边、发光和后处理。

Three.js 没有业务选择或高亮概念。应用通常遍历选中 Mesh、修改 emissive/color，或把对象放进单独的 OutlinePass/Bloom pass。

## 6.7 对比结论

| 对比点 | HT | Three.js |
|---|---|---|
| 材质载体 | Data 的 shape3d 样式 | Material 实例 |
| 默认值 | 全局样式表和分面默认值 | Material 构造函数默认值 |
| 绑定 | s()/a() 可直接绑定业务值 | 自己监听业务数据并写材质 |
| 分面材质 | top/bottom/from/to 样式 | geometry groups + 多材质 |
| 灯光选择 | light.mask 和样式 | 场景层级/visible/自定义 shader |
| 高亮 | 选择模型、描边和后处理内建 | 需要应用或 examples 组合 |
