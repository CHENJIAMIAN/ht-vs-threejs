# 01 架构总览与版本同源性

> 对比对象：HT for Web 与 Three.js r125；含 shader 版本同源性的代码指纹分析。

## 1.1 两个不同的产品层级

HT 是业务可视化框架。它把数据模型、2D 拓扑、3D 渲染、交互、组件和工程文件放在同一个运行时里。Three.js 是通用 3D 库，提供场景对象、几何、材质、灯光、相机和 WebGL 提交机制，不定义业务数据和 2D 拓扑。

HT 的最小业务路径是：

    DataModel -> Data -> Node/Edge/Group/Shape -> GraphView 或 Graph3dView -> Canvas/WebGL

Three.js 的最小渲染路径是：

    Scene -> Object3D/Mesh -> WebGLRenderer.render(scene, camera)

因此，HT 与 Three.js 的比较应分成两层：数据和业务能力比较，外加 WebGL shader 与数学实现的同源性比较。不能把 HT 的 Node 直接等同于 Three.js 的 Mesh：Node 同时承载业务属性、样式、父子关系和事件，Mesh 只连接几何体与材质。

## 1.2 HT 当前源码结构

入口是 `src/compat/entry-ht.mjs`。它导入 `src/utils/bootstrap-clock.js`、`src/bootstrap/core-bootstrap.js`、`src/utils/polyfills.js` 和 `src/bootstrap/bootstrap-runtime.js`；esbuild 沿依赖图生成 `dist/ht.js`，构建不依赖目录清单。

| 功能域 | 当前目录 | 代表文件 |
|---|---|---|
| 启动 | `src/bootstrap/` | core-bootstrap.js、bootstrap-runtime.js、runtime-registration.js |
| 核心 | `src/core/` | ht-namespace.js、default-config.js、data-resource-utilities.js、heartbeat.js |
| 数学 | `src/math/` | vector2.js、vector3.js、quaternion.js、matrix4.js、line3.js、easing.js |
| 动画 | `src/animation/` | animation-namespace.js、animation-track.js、animation-runtime.js、binding.js |
| 几何 | `src/geometry/` | shape-extrude.js、runtime-geometry-surfaces.js |
| 渲染底层 | `src/renderers/` | program-manager.js、shaders-library-lighting.js、shadow-map.js |
| 2D 画布 | `src/canvas/` | canvas-util-functions.js、runtime-canvas-drawing.js |
| 图模型 | `src/graph/` | data.js、node.js、graph-view.js、notifier2.js |
| 3D 图形 | `src/graph3d/` | graph3d-view.js、graph3d-render-list.js、prefab-renderer.js |
| 组件 | `src/widget/` | widget-form-controls.js、widget-list-accordion-views.js |
| 工具 | `src/utils/` | bootstrap-clock.js、literal-anchors.js、float32-array-pool.js |

`src/core/`、`src/graph/`、`src/graph3d/` 和 `src/animation/` 中现在是可以直接阅读的 ES module；例如 `src/graph/data.js` 直接导出 `Data`，`src/graph/node.js` 直接导出 `Node extends Data`，`src/graph3d/graph3d-view.js` 直接导出 `Graph3dView`，`src/animation/animation-namespace.js` 组装 `ht.Animation`。

## 1.3 全局注册和启动

`src/bootstrap/core-bootstrap.js` 创建 `globalThis.ht` 和默认配置，`src/core/ht-namespace.js` 向其他模块提供同一个命名空间引用。图模型、3D 图形、组件和动画模块在加载时把公开类挂到相应命名空间：

    globalThis.ht
    ├─ Default / Style / Attr / Color
    ├─ Data / DataModel / Node / Edge / Group / Shape
    ├─ graph.GraphView
    ├─ graph3d.Graph3dView
    ├─ Animation / Binding
    └─ widget.*

这种设计保留了传统 script 加载 `dist/ht.js` 的兼容入口，同时让源码内部使用静态 import。Three.js 则由入口模块导出 `THREE` 对象，类之间通过 import 连接，不提供 HT 式业务命名空间。

HT 的类注册语法糖是 `def(...)`：把类的方法表、静态元数据与原型一次性挂好，是 HT 组织对外 API 的统一入口。

## 1.4 Three.js r125 的模块边界

Three.js r125 的 `src/` 按渲染库职责分层：`core/Object3D.js` 是对象基类，`scenes/Scene.js` 是场景根，`objects/` 保存 Mesh/Line/Sprite，`geometries/` 保存几何体，`materials/` 保存材质，`cameras/` 和 `lights/` 保存相机/灯光，`renderers/` 保存 WebGL 管线，`math/` 保存数学类，`loaders/` 和 `animation/` 是扩展能力。

Three.js 不会因为 `object.position.x` 或 `object.userData.temperature` 改变而向业务视图广播事件，也不会替应用决定何时重绘。应用通常自己写 `requestAnimationFrame`，自己维护数据到 Object3D 的映射。

## 1.5 shader 同源性

HT 当前 shader 位于 `src/renderers/shaders-library-core.js`、`shaders-library-lighting.js`、`shaders-library-features.js`、`shaders-materials.js`、`shaders-effects.js` 和 `shaders-postprocessing.js`。`src/renderers/program-manager.js` 负责宏注入、include 展开、循环展开和程序缓存。

| 指纹 | HT 当前文件 | Three.js 对照 | 结论 |
|---|---|---|---|
| DFGApprox 与 0.047619 多散射 | shaders-library-lighting.js | r125 lights_physical_pars_fragment.glsl.js | 公式逐字一致 |
| computeSpecularOcclusion | shaders-library-lighting.js | r125 同名函数 | 公式逐字一致 |
| cubeUV 的 8.0/256.0/16.0 | shaders-library-lighting.js | r125 cube_uv_reflection_fragment.glsl.js | 布局一致 |
| specularF90 和六参 BRDF_GGX | shaders-library-lighting.js | r133+ | HT 不是 r125 旧签名 |
| IBLSheenBRDF | shaders-library-lighting.js | r136 | HT 含 r136 才出现的 sheen IBL |
| transmission vec4/attenuationColor | shaders-library-lighting.js | r132～r136 | HT 使用新透射实现 |
| fresnelIntensity（业务扩展） | shaders-library-core.js | 无对应 | HT 自己在菲涅尔结果上乘系数 |

> 2026-09-22 复核：以上指纹逐条在当前 `src/renderers/shaders-library-lighting.js` 中重新命中；
> `BRDF_GGX` 的实际调用点是六参数形式
> `BRDF_GGX(lightDir, viewDir, normal, specularColor, specularF90, roughness)`，与 r125 的旧签名不同。
> `fresnelIntensity` 不在 lighting 文件里，而在 `shaders-library-core.js`（本轮更正）。

综合指纹把 HT 的 shader 同源区间定位在 r133～r136。HT 还增加了 fresnelIntensity、IOR/transmission 字段、自己的宏命名和业务 uniform；所以更准确的说法是「Three.js shader 体系的移植与二次开发」，而不是完整复制 Three.js 渲染器。

## 1.6 对比基线的说明

- HT 侧以业务演示工程内的 `src/**` 实现为准；文中引用的 `ht.*` 类名与方法名与公开 API 文档一致，便于交叉核对。
- Three.js 侧固定为 r125（`three-0.125.0/package/src/**`），保证每个结论都能在原文件里找到对应实现。
- HT 的 shader 指纹落在 Three.js r133～r136，晚于 r125。跨版本比对时以 r125 的 shader 文件作落点，同时标注该片段实际对应的版本区间。
- 对比只讨论两边的实现方式差异，不对第三方库做优劣评判。
## 1.7 对比结论

1. HT 的核心是数据模型驱动的双视图业务框架，Three.js 的核心是可组合的 3D 渲染对象。
2. HT 当前源码已经按功能域拆开，构建入口是 `src/compat/entry-ht.mjs`，公开类由正常 ES module 导出。
3. 两者的数学类和部分 shader 高度同源，但场景管理、业务属性、2D 拓扑、拾取、阴影调度和组件体系不是同一个设计。
4. HT 侧新增的 `ht.Animation` 时间轴/轨道/绑定体系，是两边目前最接近「同类系统」的一处（对照见 14 章），但它服务的目标对象仍不同：HT 绑定 `Data` 的属性与样式，Three.js 绑定 `Object3D` 与材质字段。
