# 04 2D 拓扑渲染与组件

> 对比对象：HT 的 2D 拓扑视图与组件体系，对照 Three.js r125。

## 4.1 HT 的 2D 是一等能力

HT 的 2D 不是把 3D 场景换成正交相机，而是独立的拓扑绘制引擎。DataModel 保存节点、连线、分组和形状；src/graph/graph-view.js 负责视口、图层、脏矩形、数据 UI 缓存、编辑和拾取；src/canvas/ 负责 Canvas 2D 路径和文字绘制。

Three.js 没有 Node、Edge、拓扑路由或属性面板。用 Three.js 做 2D 业务图，通常要额外实现数据模型、连线布局、文字、选中、编辑和 DOM 组件。

## 4.2 GraphView 绘制链

GraphView 通过 DataModel 监听数据变化。数据或样式改变后，对应 Data/UI 被标为失效，下一次绘制只处理需要更新的区域。src/canvas/runtime-canvas-drawing.js 和 src/canvas/canvas-util-functions.js 提供：

- 矩形、圆角矩形、椭圆、多边形、星形、圆弧和自定义路径；
- 文字测量、换行、对齐、裁剪和图片模式填充；
- 线宽、虚线、渐变、图片描边和设备像素比处理；
- 绘制前后的 Canvas 状态保存与恢复。

节点 UI 和连线 UI 位于 src/graph/interactor-guides.js、src/graph/view-node-utilities.js、src/graph/view-edge-ui.js 等模块。业务代码只设置图元属性：

    const node = new ht.Node();
    node.p(120, 80);
    node.s({
      "shape": "roundRect",
      "shape.background": "#164e63",
      "label": "泵站"
    });
    dataModel.add(node);

## 4.3 Edge、Shape 和 Group

src/graph/edge.js 维护 source/target 双向连接，src/graph/graph-node-geometry.js 计算直线、折线、贝塞尔、圆弧、正交和自环路径。节点移动时，Edge 会根据端点和路由类型重新计算，业务代码不必重建几何缓冲。

src/graph/shape.js 保存 points/segments，支持多段线和贝塞尔曲线；src/graph/group.js 维护子节点、折叠状态和子节点包围盒；src/graph/block.js 允许一个数据项递归包含多个图元。

## 4.4 拾取、编辑和图层

GraphView 的拾取是数据级操作：先按绘制顺序查找候选 Data，再结合包围盒、路径和像素信息确认目标。选择由 src/graph/selection-model.js 管理，选中状态通过样式和 UI 反馈。

src/graph/interactor.js、interactor-editing.js、interactor-guides.js 和 interactor-transforms.js 组成编辑器：拖动、框选、旋转、缩放、吸附、调整组边界和曲线控制点都以 Data 属性为最终结果。

图层通过 Data.setRenderLayer()、render layer mask 和 GraphView 的绘制顺序控制。图层是业务数据的一部分，所以同一节点在 2D 和 3D 中可以使用不同的可见性规则。

## 4.5 Widget 组件

src/widget/ 是与 2D/3D 视图联动的组件域：

| 组件 | 当前文件 | 用途 |
|---|---|---|
| 表单控件 | widget-form-controls.js | 输入、按钮、选择、飞行定位等 |
| 树与列表 | tree-view.js、list-view.js、widget-list-accordion-views.js | 数据浏览和导航 |
| 属性面板 | property-view.js、property.js | 读取和编辑 Data 的属性/样式 |
| 表格 | table-view.js、table-views.js | 业务表格和单元格编辑 |
| 工具栏/面板 | toolbar.js、border-pane.js | 组合页面布局 |
| 图标 | icons-core.js、icons-guides.js、vector-icons-utilities.js | 组件和编辑器图标 |

这些组件通过 DataModel、SelectionModel 和属性事件联动。Three.js 的 examples/jsm 提供少量控件，但没有与场景数据统一的 widget 体系。

## 4.6 对比结论

| 能力 | HT | Three.js |
|---|---|---|
| 节点/连线语义 | 内建 Node、Edge、Group、Shape | 需要自己定义 |
| 路由 | 直线、折线、贝塞尔、正交、自环 | 只有几何线段，路由需自研 |
| 绘制更新 | 属性事件 + 脏矩形 | 应用自行标记并重绘 |
| 2D 拾取 | Data/UI 级查找 | 无 2D 业务拾取 |
| 编辑器 | 交互器和辅助线内建 | 需组合 DOM/Canvas/控制器 |
| 组件 | ht.widget 与 DataModel 联动 | 无对应核心模块 |

HT 的优势不在 Canvas API 更复杂，而在于它把拓扑语义、绘制、交互和组件连成了同一套数据系统。

## 4.7 流动效果归属（避免找错 API）

管道流动、能量输送这类「沿路径滚动的贴图」在 HT 里属于 **3D 侧**的能力：

- 渲染实现在 `src/graph3d/flow-renderer.js`（读 `effect.flow`、gradient、uvshape、size、intensity，维护 flow texture / flow matrix / 时间偏移），配合 `src/renderers/shaders-effects.js` 与 `shaders-materials.js` 在 shader 里滚动 UV；
- 业务侧入口是节点的 `effect.flow` 系列样式，见 13.4 节。

`GraphView`（2D 拓扑视图）本身没有 `enableFlow` / `disableFlow` 这类方法——在源码里检索这两个名字不会有结果，不要按名字去 2D 视图里找。2D 的「流动」观感通常由连线的 dash、渐变或自定义 `render` 回调实现。

