# 10 后处理

> 对比对象：HT 的内建后处理模块，对照 Three.js r125 `examples/jsm/postprocessing`。

## 10.1 管线差异

HT 把后处理做成 Graph3dView 的内建模块。src/graph3d/postprocessing.js 导出 PostProcessing，创建全屏网格、RenderTarget、材质和模块状态，并按固定阶段运行。

Three.js r125 的后处理主要在 examples/jsm/postprocessing/EffectComposer.js 和各类 Pass 中。EffectComposer 提供双缓冲链，但由应用决定启用哪些 Pass、顺序和 RenderTarget。

## 10.2 HT 的模块

src/graph3d/postprocessing-effects.js 导出以下模块：

| 模块 | 功能 |
|---|---|
| HueSaturation | 色相、饱和度 |
| Bloom | 高光提取、水平/垂直高斯模糊、加法合成 |
| Dof | 景深和 bokeh |
| Lut | 颜色查找表 |
| Custom | 用户自定义 shader/pass |
| Blur | 全屏模糊，也被 skybox 复用 |
| Glitch | 相位、抖动和扫描线 |
| Fxaa | NVIDIA FXAA 3.11 |

PostProcessing 维护 enabled、initialized、当前模块、全屏 mesh、读写 RenderTarget 和最终合成。sceneBloom*、sceneDof*、sceneLut* 等属性可以序列化到 DataModel 的 postProcessingData。

## 10.3 Bloom、FXAA 和景深

src/renderers/shaders-effects.js 保存高光阈值、Gaussian PDF、模糊采样、Bloom 合成、FXAA 和 bokeh shader。HT Bloom 的高光提取、两方向模糊和加权合成与 Three.js UnrealBloomPass 同源；FXAA 使用同一 NVIDIA 3.11 思路。

HT 的 Bloom 还可以按 Node 的 bloom 样式选择性加入，src/graph3d/graph3d-render-list.js 和 PostProcessing 共同控制哪些渲染层进入高光 pass。Three.js 的 UnrealBloomPass 通常对整个输入纹理处理，选择性 bloom 需要应用额外的 layer/camera。

HT 的 Dof 模块使用焦距、焦点、孔径和 bokeh 采样。Three.js r125 的 BokehPass/BokehShader 提供相近算法，但没有 HT 的 DataModel 属性绑定。

## 10.4 色调映射和最终输出

HT 的 tone mapping 位于材质 shader 输出路径，src/renderers/shaders-library-lighting.js 提供 Reinhard、Cineon、ACES 等函数；PostProcessing 负责 Bloom/DOF 等纹理处理后再回到最终输出。

Three.js r125 的 toneMapping、toneMappingExposure 和 outputEncoding 是 WebGLRenderer 全局设置，色调映射通常在材质输出 shader 中完成。r125 没有后来版本的 OutputPass，EffectComposer 用户需要自己安排编码和最终 pass。

## 10.5 RenderTarget 和采样

HT 的 RenderTarget 位于 src/graph3d/render-target.js，图形状态和绑定辅助位于 src/renderers/render-target.js、runtime-render-helpers.js。Bloom、DOF、环境转换、拾取和阴影都可以从同一类 RenderTarget 获取、复用和释放。

Three.js 使用 WebGLRenderTarget、WebGLMultipleRenderTargets 和 EffectComposer 的 readBuffer/writeBuffer。r125 的 WebGLRenderTarget 没有后续版本的通用多采样参数，MSAA 通常由平台或额外实现处理。

## 10.6 对比结论

| 对比点 | HT | Three.js r125 |
|---|---|---|
| 组织方式 | PostProcessing 固定模块表 | EffectComposer + Pass 自由组合 |
| 模块开关 | DataModel 场景属性，可序列化 | 应用代码维护 |
| Bloom | 选择性 bloom、三件套、场景层联动 | UnrealBloomPass，默认处理输入纹理 |
| 景深 | Dof 模块和业务参数 | BokehPass/BokehShader |
| FXAA | 内建最终模块 | ShaderPass + FXAAShader |
| Glitch/LUT/Custom | 内建模块 | 需自行选择或编写 Pass |
| RenderTarget | 统一池、绑定和释放 | Composer 双缓冲，由应用管理 |

