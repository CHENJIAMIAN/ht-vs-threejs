# 09 环境贴图与天空盒

> 对比对象：HT 的环境贴图与天空盒，对照 Three.js r125。

## 9.1 环境贴图在两套系统中的位置

环境贴图既可以作为物体的镜面/漫反射光源，也可以作为场景背景。HT 把这两件事纳入 Graph3dView 和 DataModel 的场景状态；Three.js 把它们分别暴露为 scene.environment、scene.background 和 PMREMGenerator 工具。

HT 的数据模型可以保存 envmap、envmapType、skybox 和 probe 配置。Node 的 shape3d.envmap/probe 样式选择要采样的环境。Three.js 的 MeshStandardMaterial.envMap 和 Scene.environment 则由应用代码直接赋 Texture。

## 9.2 HT 的环境贴图管线

src/graph3d/envmap-renderer.js 导出 EnvmapRenderer，维护环境贴图缓存、probe 捕获时间、渲染目标池和强度表。它支持：

| 来源 | HT 处理 |
|---|---|
| equirectangular | 等距柱状图转换为 cubemap，再生成预过滤 mip |
| cubemap | 直接上传六面纹理并转换为 cubeUV |
| skybox | 从 skybox 节点的球面图或六面图生成环境 |
| scene probe | 用指定位置、范围和更新策略捕获场景 |
| legacy image | 兼容单张环境图片，上传到六个 cube face |

src/renderers/envmap-lods.js 生成 PMREM 的 LOD 平面、尺寸、sigma 表和六面方向。src/renderers/shaders-postprocessing.js 中的 envmapEquirect、envmapCubemap 和 envmapBlur 程序完成等距投影、立方体面转换和模糊。

环境更新不是每帧无条件重建。EnvmapRenderer 根据 probe 配置、源图片 hash、当前帧和图像就绪状态决定是否重新捕获/转换，并复用 render target。

## 9.3 cubeUV 和 PMREM

HT 的 PBR shader 从 cubeUV 纹理读取环境。cubeUV 把多个 mip 和六个面排布在一张二维纹理中，shader 根据方向和 roughness 计算 face、tile、mip 和双线性采样坐标。

src/renderers/shaders-library-lighting.js 中保留了 Three.js 的 cubeUV 常量和布局公式：

    cubeUV_maxMipLevel = 8.0
    cubeUV_maxTileSize = 256.0
    cubeUV_minTileSize = 16.0

DFG 近似、roughnessToMip、bilinearCubeUV 和 textureCubeUV 的数学与 Three.js r125/r133～r136 高度同源。HT 在外层加入了场景 probe、缓存、强度和材质/分面样式。

## 9.4 HT 的天空盒

天空盒是 Graph3dView 的一个专用 Data/Node。src/graph3d/graph3d-view.js 读取 skybox 节点和 sceneSkybox* 属性，src/graph3d/envmap-renderer.js 负责将其作为背景或 probe 来源。

天空盒支持：

- sphere：一张等距柱状图贴到球面；
- cube：六张图片组成立方体；
- color：纯色背景；
- blur.size 和 blur.resolution：调用环境/后处理模糊路径；
- 跟随相机、不参与普通深度遮挡，场景物体可以覆盖天空盒。

## 9.5 Three.js r125

Three.js r125 使用：

- PMREMGenerator.fromEquirectangular() 或 fromCubemap() 生成预过滤环境；
- scene.environment 作为 PBR 材质的 IBL 来源；
- scene.background 作为颜色、2D 纹理或 CubeTexture 背景；
- CubeTextureLoader 从六个 URL 加载 CubeTexture；
- WebGLCubeRenderTarget 和 CubeCamera 做实时立方捕获。

r125 没有 sceneSkyboxType、probe 缓存、skybox blur 或自动场景捕获策略。应用需要自己创建 CubeCamera、RenderTarget 和更新时机。

## 9.6 对比结论

| 对比点 | HT | Three.js r125 |
|---|---|---|
| 环境配置 | DataModel 场景属性 + shape3d 样式 | scene.environment + material.envMap |
| 输入 | 等距图、cubemap、skybox、probe、legacy | Texture/CubeTexture/RenderTarget |
| PMREM | EnvmapRenderer 自动转换和缓存 | PMREMGenerator 工具类 |
| cubeUV | shader 内建并和 Three.js 同源 | PMREM 输出给材质采样 |
| 天空盒 | sphere/cube/color 节点，支持模糊 | scene.background，r125 无模糊属性 |
| 动态 probe | 场景属性配置、按需捕获 | CubeCamera 由应用驱动 |
| 业务绑定 | envmap/probe 可随 Data 属性变化 | 需要自己监听业务数据 |

HT 的 PMREM 核心算法来自 Three.js 体系，但自动化的 probe、skybox 和工程属性是 HT 的业务层扩展。

