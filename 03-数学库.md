# 03 数学库

> 对比对象：HT 与 Three.js r125 的数学库实现。

## 3.1 同源范围

HT 的 Vector、Matrix、Quaternion、Euler、Box3、Sphere、Ray、Frustum 和插值器已经位于同一个 src/math/ 功能域。Three.js r125 也有对应的数学目录。两边的类方法、链式返回和列主序矩阵大量相似，能够直接进行逐方法迁移对照。

共同约定：

1. 向量和矩阵方法通常原地修改对象并返回 this。
2. Matrix4 使用列主序数组；索引 0、4、8、12 是第一列。
3. multiply() 是 this * argument，premultiply() 是 argument * this。
4. Quaternion 使用 _x/_y/_z/_w 形式的内部分量，支持球面插值和由轴角、欧拉角、旋转矩阵构造。

## 3.2 HT 当前实现

| 类 | 当前文件 | HT 特点 |
|---|---|---|
| Vector2 | src/math/vector2.js | x/y 外还有 e 高程分量，服务 2D 拓扑和地图数据 |
| Vector3 | src/math/vector3.js | 矩阵、四元数、欧拉角、方向向量变换 |
| Vector4 | src/math/vector4.js | 四分量向量和颜色/齐次计算 |
| Quaternion | src/math/quaternion.js | 轴角、欧拉角、矩阵、slerp 和批量插值 |
| Matrix2d | src/math/matrix2d.js | 2D 仿射变换 |
| Matrix3 | src/math/matrix3.js | 3×3 旋转、法线和二维辅助矩阵 |
| Matrix4 | src/math/matrix4.js | 投影、视图、模型、组合和分解 |
| Box3/Sphere/Ray/Frustum | src/math/ 对应文件 | 三维包围体、相交和视锥裁剪 |
| 插值器 | src/math/*-interpolant.js | 线性、离散、三次、四元数和连续采样 |

Vector3.applyMatrix4() 使用 el 数组按列主序计算齐次坐标；Matrix4.multiply()、premultiply()、compose() 和 decompose() 的语义与 Three.js 对应方法一致。src/math/runtime-math-interpolants.js 还提供矩阵对象池，减少渲染帧中的临时分配。

## 3.3 HT 与 Three.js 的重要差异

### Euler 默认顺序

HT 的 Euler 位于 src/core/euler.js，默认顺序是 ZYX；Three.js r125 的 Euler 默认顺序是 XYZ。这是迁移旋转数据时最容易出现的视觉差异。若两端交换欧拉角，必须显式指定顺序；更稳妥的方式是交换四元数。

### Vector2 的高程

HT Vector2 的 e 字段不是 Three.js Vector2 的标准成员。它用于 2D 拓扑中表达高程/层级信息，不能直接把 HT Vector2 当成 Three.js Vector2 传入后期待第三维仍然存在。

### 矩阵字段

HT 使用 el，Three.js 使用 elements。存储布局相同，但直接访问数组的代码不能原样复制。

### 世界矩阵 API

Three.js Object3D 公开 matrixWorld、getWorldPosition() 等接口。HT 的世界变换主要在 src/graph3d/graph3d-util-functions.js、src/graph3d/apply-model-matrix.js 和 Graph3dView 的渲染阶段组合；业务 Node 没有对应的公开世界矩阵对象。

## 3.4 Three.js 对应实现

Three.js r125 的 Vector3.applyMatrix4()、Matrix4.multiplyMatrices()、Quaternion.slerpFlat() 和 Euler.setFromRotationMatrix() 与 HT 的实现可以逐段比较。主要差异不是数学公式，而是对象字段、默认欧拉顺序、对象生命周期和上层调用方式。

## 3.5 迁移建议

| 场景 | 建议 |
|---|---|
| 交换位置/缩放 | 直接复制 x/y/z 和 scale 分量 |
| 交换旋转 | 优先交换四元数；若交换欧拉角，明确 ZYX/XYZ |
| 交换矩阵 | 复制 16 个元素，并注意 el 与 elements 字段名 |
| 交换包围盒 | Box3 的 min/max 语义相同，但方法名和构造方式需逐项核对 |
| 交换插值器 | Three.js 的 Interpolant 轨道对象不能直接替代 HT 的业务属性绑定 |

## 3.6 Line3 与缓动族

| 类 / 命名空间 | 文件 | 说明 |
|---|---|---|
| `Line3` | src/math/line3.js | 线段：`getCenter`、`delta`、`distanceSq`、`distance`、`at`、`closestPointToPointParameter`、`closestPointToPoint`、`equals`、`clone` |
| `ht.Math.Easing` | src/math/easing.js | 缓动族；基类按 `Easing.Mode`（In / Out / InOut）把 `easeImpl` 派生成三种形态 |
| `ht.Default.createEasing(name)` | src/math/easing.js | 名字 → 绑定的 ease 函数；带缓存，同一名字拿到同一个函数；空名返回 Linear |

缓动行为表含 Linear、Quadratic、Cubic、Quartic、Power、Sine、Exponential、Circle、Elastic、Back、Bounce、Bezier，另有 Expo / Pow / Quad / Quart / Sin 五个别名（别名与全名共用同一份实现）。

与 Three.js 的对照：

- `Line3` 在双方几乎逐方法同名同语义（r125 的 `src/math/Line3.js` 也有 `closestPointToPoint` / `closestPointToPointParameter`），可直接逐段比对。
- **缓动族 Three.js r125 没有**：r125 只有 `Interpolant`（曲线采样）和 `MathUtils`（插值/夹取），业务缓动通常来自 Tween.js / GSAP 之类的第三方库。HT 把缓动做进了核心，并且 `ht.Animation.Track` 的每一段都能直接引用缓动名。

## 3.7 旋转顺序有两套默认值（迁移陷阱）

同一个工程里存在两个不同的默认旋转顺序，写旋转数据时必须显式指定：

| 位置 | 默认顺序 | 出处 |
|---|---|---|
| 核心 `Euler` | `ZYX` | src/core/euler.js（构造时未指定 order 时取 `ZYX`） |
| 动画域 `ht.Animation.Util` | `YZX` | src/animation/animation-util.js（`DEFAULT_ROTATION_ORDER`） |

Three.js r125 的 `Euler` 默认是 `XYZ`，三者互不相同。跨库交换旋转数据时，最稳的做法是交换四元数；若必须交换欧拉角，三边的顺序都要写明。

## 3.8 结论

HT 数学库是与 Three.js 同源度最高的部分。底层公式和链式 API 基本一致；真正需要防范的是 Euler 默认顺序、Vector2 高程字段、矩阵字段名以及 HT 把数学对象嵌入业务图元生命周期的方式。

