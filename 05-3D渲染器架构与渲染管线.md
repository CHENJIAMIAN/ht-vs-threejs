# 05 3D 渲染器架构与渲染管线

> 对比对象：HT 的 3D 渲染管线，对照 Three.js r125 的 WebGLRenderer。

## 5.1 两条渲染路径

HT 的 3D 渲染从 DataModel 出发，先把业务图元转换为渲染模型，再把渲染模型组织成批次和渲染列表：

    DataModel
      -> Graph3dView
      -> PrefabRenderer / Shape3dUI
      -> Graph3dRenderList
      -> program-manager + shader
      -> WebGL draw

Three.js 从 Scene 出发，每帧遍历 Object3D 树，收集 Mesh、材质和灯光，排序后提交 WebGL：

    Scene
      -> WebGLRenderer.render()
      -> WebGLRenderLists
      -> WebGLPrograms / WebGLTextures
      -> WebGL draw

HT 的渲染项带有业务 Data 引用、render layer、选择和拾取信息；Three.js 的 RenderItem 主要描述对象、几何和材质。

## 5.2 Graph3dView 的职责

src/graph3d/graph3d-view.js 导出 Graph3dView。它维护：

- DataModel 和 3D 视口；
- eye、center、up、投影类型、near/far、fovy 和正交宽度；
- render layer mask、相机和雾；
- Graph3dRenderLists、PrefabRenderer、DepthRenderer、EnvmapRenderer 和 PostProcessing；
- hover、selection、编辑交互和 GPU 拾取；
- renderModel、renderScene、readPixels 等渲染入口。

Graph3dView 不把每个 Node 永久变成一个独立 Mesh。src/graph3d/prefab-renderer.js 会按 shape3d、材质、贴图、透明度和批处理条件生成或复用渲染模型；src/graph3d/shape3d-ui.js、node3d-ui.js 和 edge3d-ui.js 负责把不同 Data 类型翻译成渲染数据。

## 5.3 渲染列表、排序和批处理

src/graph3d/graph3d-render-list.js 导出 Graph3dRenderList，维护 opaque、transparent、batch、polyline 和 wireframe 等集合。src/graph3d/graph3d-render-lists.js 管理不同 render layer 的列表。

一个渲染项通常包含：

| 字段 | 作用 |
|---|---|
| data | 对应的 HT Data |
| ui | Node3dUI、Shape3dUI 或 Edge3dUI |
| model | 顶点、法线、UV、索引等几何模型 |
| material | 材质类型和样式解析结果 |
| matrix | 模型变换矩阵 |
| batchInfo | 可合批模型的偏移和索引 |
| pickColor | GPU 拾取用的对象颜色 |

相同几何和材质的对象会进入 batch 或 instanced batch；透明、线框、流动纹理和不同渲染层会拆开。批处理更新由 updateInstanceData、updateBatchColors、removeFromBatch 等模块完成。

Three.js r125 的 WebGLRenderLists 也会把对象按 opaque/transparent 分类并排序，但合批通常需要应用显式创建 InstancedMesh、合并 BufferGeometry 或使用自定义插件。Three.js 不会根据业务样式自动决定哪些 Node 可以合批。

## 5.4 程序和 shader

src/renderers/program-manager.js 根据材质类型、灯光、阴影、环境贴图、透明和后处理开关生成程序定义。src/renderers/shaders-materials.js 提供 PBR、Phong、Lambert、拾取、深度、环境贴图和全屏 blit 模板；src/renderers/shaders-library-*.js 提供可复用 shader chunk。

HT 的程序缓存键包含材质和场景开关，避免每帧重新拼接 shader。shader 通过 include 名称组合，program-manager 再展开 include、宏和固定次数循环。

Three.js r125 使用 WebGLPrograms 和 resolveIncludes 组合 ShaderChunk/ShaderLib，并按 material、灯光、阴影和 renderer 状态生成缓存键。两者的 chunk 思路接近，但 HT 的 chunk 同时承载业务批处理、流动、OIT 和对象拾取分支。

## 5.5 UBO、纹理和透明

src/renderers/uniforms.js、runtime-webgl-descriptors.js 和 src/graph3d/uniform-buffer.js 负责 uniform 描述、WebGL 能力、纹理单元和 UBO。场景级和材质级值可以批量上传，减少重复 uniform 调用。

透明对象根据 style 的 opacity、alphaTest、blend 和 render layer 分类。HT 的 OIT 路径由 src/graph3d/envmap-oit-renderer.js、postprocessing.js 和 shader 中的 OIT chunk 共同完成，可将多个透明对象的颜色/权重延迟合成。

Three.js r125 默认依赖深度测试和透明排序；没有 HT 这种面向业务场景的内建 OIT 管线。需要加权混合时，通常由应用接入后处理示例或自定义材质。

## 5.6 渲染循环

HT 的普通数据变化会使 GraphView/Graph3dView 失效，随后调度一次重绘；相机交互、动画、环境 probe 和持续流动效果才会保持连续更新。src/core/core-json-browser-utilities.js 的 startAnim 与 src/core/core-deferred-callbacks.js 的 callLater 提供统一调度。

Three.js 的典型写法是应用自己维护 RAF：

    function animate() {
      requestAnimationFrame(animate);
      renderer.render(scene, camera);
    }

HT 也可以持续渲染，但“是否需要重绘”的判断属于视图和 DataModel，Three.js 则把渲染循环控制权留给应用。

## 5.7 心跳、限流与阻尼

渲染之外还有三件支撑件，负责「什么时候该跑」和「跑多快」，都由核心提供：

| 能力 | 文件 | 机制 |
|---|---|---|
| 全局逐帧心跳 | src/core/heartbeat.js | `ht.Default.registerHeartbeat` / `unregisterHeartbeat`；回调按 `priority` 从大到小排序，每帧收到 `(timestamp, delta)` |
| 帧率上限 | src/graph/fps-limiter.js | `createFpsLimiter(limit)` 判断本帧是否允许渲染；`limit <= 0` 表示不限流 |
| 交互惯性 | src/graph3d/interactor-damping.js | 交互位移先累加到 `_rotateDelta` / `_panDelta` / `_scaleDelta`，再由 rAF 循环按 `_dampingFactor` 逐帧衰减施加 |

两个容易被忽略的细节：

- 心跳里某个回调抛异常时，有 `onError` 就交给它，否则**把该回调移除**并 `console.error`；没有回调时心跳自行停掉 rAF，不空转。
- 限流器在掉帧后会把基准时间对齐到最近的整周期，避免误差逐帧累积成持续漂移（`lastCheckTime += Math.floor((now - lastCheckTime) / interval) * interval`）。

与 Three.js 的对照：r125 的对应物是 `renderer.setAnimationLoop` 与「应用自管 `requestAnimationFrame`」，渲染节奏完全由应用决定；阻尼惯性只存在于 `OrbitControls` 这类 examples 控件内部（`enableDamping` + 每帧 `update()`），不是渲染器级能力。HT 把这三件事做进了核心：业务只需要声明「什么变了」，由视图决定何时重绘。

## 5.8 对比结论

| 对比点 | HT | Three.js |
|---|---|---|
| 输入 | DataModel 和业务样式 | Scene/Object3D |
| 渲染对象 | 按需生成、可复用的 prefab/render item | Mesh 显式持有 geometry/material |
| 合批 | 根据模型、材质和样式自动分类 | 应用显式合并或实例化 |
| 透明 | OIT、渲染层和透明列表 | 深度测试 + 排序为主 |
| UBO | 场景/材质级描述和批量上传 | r125 主要逐 uniform 上传 |
| 更新 | 数据失效驱动 + 连续效果按需运行 | 应用通常每帧 render |
| 拾取 | 独立 GPU pass | 通常 Raycaster CPU 求交 |