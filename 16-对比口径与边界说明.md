# 16 对比口径与边界说明

> 本文说明这套对比的判定原则、可比范围与常见误读，避免把「像」当成「是」。

## 16.1 判定原则

1. **比实现，不比宣传**：每条结论都要能落到具体的类、方法或 shader 片段上。
2. **区分「同源」与「相同」**：公式逐字一致叫同源；调用方式、数据归属、调度时机完全不同时，即使公式一样，也不是同一套系统。
3. **区分「层级差异」与「实现差异」**：HT 有 2D 拓扑、数据模型、组件体系，Three.js 没有——这是层级差异，不该当成实现优劣；两边都有的东西（数学库、shader、后处理）才是实现差异，值得逐段对照。
4. **给对照表，不给结论式形容词**：每章末尾用表格列出「对比点 / HT / Three.js」，读的人自己能判断。

## 16.2 可比范围

| 可比 | 说明 |
|---|---|
| 数学库 | 向量、矩阵、四元数、包围体、插值器：方法名与语义大量对应，可逐方法比对 |
| shader 体系 | PBR、Phong、GGX/Smith/Schlick、多散射、cubeUV/PMREM、FXAA、Bloom/景深片段，可逐函数比对 |
| 后处理与阴影 | 都是 shadow map 与全屏 pass 的经典做法，差异在资源归属与调度 |
| 渲染调度 | 一边是数据失效驱动 + 按需重绘，一边是应用每帧 `render`，这是最值得理解的差异 |

| 不可比（各说各的） | 说明 |
|---|---|
| 2D 拓扑 | Three.js 没有对应概念，谈不上对比 |
| 业务数据模型与序列化 | 属于业务框架能力，不是渲染库的职责 |
| 组件体系（表格、树、属性面板） | 同上 |
| 产品生态与许可 | 商业产品与开源库，不在技术对比范围 |

## 16.3 常见误读

1. **「HT 的 Node 就是 Three.js 的 Mesh」**——不是。Node 同时承载业务属性、样式、父子关系、事件与序列化；Mesh 只连接几何体与材质。
2. **「shader 同源 ⇒ 渲染器同源」**——不是。HT 自己重写了程序管理、宏注入、批处理、拾取与 OIT，shader 只是其中被复用的数学与光照片段。
3. **「数学库一样 ⇒ 行为一样」**——不是。默认欧拉顺序（HT 核心 `ZYX`、动画域 `YZX`、Three.js `XYZ`）、矩阵字段名（`el` vs `elements`）、以及数学对象嵌在业务图元生命周期里的方式，都会产生肉眼可见的差异。
4. **「HT 也能每帧 render，所以调度方式一样」**——不是。能连续渲染不等于默认连续渲染；HT 的默认路径是数据失效触发重绘，连续更新只留给相机交互、动画与流动效果。
5. **「Three.js 动画更强」**——要分场景。资产动画（骨骼、蒙皮、morph）Three.js 更完整；「业务值变了画面跟着变」这类需求，HT 的绑定与自动重绘更省事（见 13、14 章）。

## 16.4 想自己复核时

- **HT 侧**：以公开 API 文档为准核对类名与成员；行为层面的结论（默认值、调度时机）以实际运行为准，不要只看文档描述。
- **Three.js 侧**：固定到一个版本（本套文档用 r125），直接读 `src/**` 源码；跨版本比对时先确认该文件在该版本是否存在、签名是否变化。
- **跨库结论**：任何「可以照搬」的建议都要在同数据同配置下跑一遍视觉与交互对照，尤其是旋转、透明、阴影和色调映射这四类最容易出现「参数一样但看起来不一样」。