# 02 场景图与数据模型

> 对比对象：HT 的数据模型与图元体系，对照 Three.js r125 的 Object3D 树。

## 2.1 核心差异

HT 把业务数据和图元放在同一对象上；Three.js 把渲染对象和业务数据分开。

    HT:     DataModel -> Data -> Node/Edge/Group/Shape -> GraphView/Graph3dView
    Three:  Scene -> Object3D -> Mesh/Line/Sprite -> WebGLRenderer

HT 的一份 DataModel 可以同时被 2D 和 3D 视图读取。Three.js 的 Scene 只描述一次渲染树；如果同一业务对象还要显示在 2D 画布、表格或属性面板中，应用必须自己维护第二套数据结构或映射表。

## 2.2 HT 的 Data 和 DataModel

src/graph/data.js 导出 Data。它提供统一的 id、父子关系、样式、业务属性、事件和序列化入口：

    const node = new Node();
    node.s("shape3d", "cylinder");
    node.a("temperature", 72);
    node.addPropertyChangeListener((event) => {
      // event.property 可能是样式、业务属性、parent 或 children
    });

s() 访问样式，a() 访问业务属性；firePropertyChange() 把改变广播给 DataModel 和视图。样式未在对象上设置时，Data.getStyle() 会回落到 ht.Style 的默认表。属性未直接设置时，getAttr() 会通过核心资源工具解析默认值和类型。

src/graph/data-model.js 导出 DataModel。构造函数创建 List、id 索引、根节点列表、选择模型和多个 Notifier。DataModel 负责添加/删除数据、维护根节点、转发属性事件、管理选择和调度延迟任务。

## 2.3 HT 的图元类型

| 类型 | 当前源码 | 作用 |
|---|---|---|
| Data | src/graph/data.js | 所有业务数据的共同基类 |
| Node | src/graph/node.js | 位置、尺寸、旋转、样式和 2D/3D 图形 |
| Edge | src/graph/edge.js | source/target 连接、路径和连线样式 |
| Group | src/graph/group.js | 子节点分组、折叠和包围盒 |
| Shape | src/graph/shape.js | 多边形、贝塞尔和自定义路径 |
| Block | src/graph/block.js | 可递归包含子图元的块容器 |
| Camera | src/graph/camera.js | 可放入 DataModel 的相机节点 |
| Light | src/graph3d/light.js | 可放入 DataModel 的 3D 灯光节点 |

Node 的 p()/p3()、r()/r3()、s()/s3() 等方法写入业务图元状态，而不是直接改一个 Mesh 的矩阵。src/graph3d/prefab-renderer.js 和 src/graph3d/shape3d-ui.js 再把这些状态转换成几何、材质、矩阵和绘制批次。

## 2.4 HT 的双视图

src/graph/graph-view.js 是 Canvas 2D 视图。它监听 DataModel 事件，维护数据 UI 缓存、脏矩形、图层和 2D 拾取。

src/graph3d/graph3d-view.js 是 WebGL 3D 视图。它读取相同的 DataModel，把 Node/Edge 转成 3D 渲染项，交给 Graph3dRenderList、PrefabRenderer 和 shader 管线绘制。

业务代码只修改一次数据：

    node.p3(10, 0, 4);
    node.a("temperature", 80);
    node.s("shape3d.color", "#ff6600");

2D 视图可以读取位置和样式，3D 视图可以读取三维位置和材质样式，widget 可以读取业务属性。这个“一个数据对象，多种观察方式”是 HT 与 Three.js 的最大架构差异。

## 2.5 Three.js 的 Object3D 树

Three.js r125 的 three-0.125.0/package/src/core/Object3D.js 维护 parent、children、matrix、matrixWorld、position、quaternion、scale 和 userData。Scene 继承 Object3D，Mesh 继承 Object3D 并额外持有 geometry/material。

    const scene = new THREE.Scene();
    const mesh = new THREE.Mesh(geometry, material);
    mesh.userData.temperature = 80;
    scene.add(mesh);
    renderer.render(scene, camera);

Object3D 的 added/removed 事件用于树结构变化；属性本身没有 HT 式样式和业务属性变更通知。matrixAutoUpdate 和 updateMatrixWorld() 解决的是渲染矩阵同步，不是业务状态广播。

Three.js 的 Object3D.toJSON() 输出对象、几何、材质和变换库，适合保存渲染场景；它不会自动保存 HT 式业务属性、数据模型选择和拓扑语义。

## 2.6 通知器：Notifier 与 Notifier2

HT 的属性变更靠一套通知器实现，定义在 `src/graph/notifier.js` 与 `src/graph/notifier2.js`：

| 类 | 文件 | 与另一个的区别 |
|---|---|---|
| `Notifier` | notifier.js | 监听项按加入顺序保存 |
| `Notifier2` | notifier2.js | 唯一区别是 `_addItem` 按监听项的 `a`（优先级）**降序插入**；`a` 为 `undefined` 时视为 0 |

`Notifier2` 是 `Notifier` 的按优先级排序版本。`Data`、`DataModel` 的属性变更、选择变化和视图失效都走这套通知器，所以「同一个 Data 被 2D、3D、表格、属性面板同时观察」在 HT 里是免费的。

对照 Three.js r125：`Object3D` 只有 `EventDispatcher` 上的 `added` / `removed` 事件，用于树结构变化；没有优先级概念，也没有业务属性变更广播。要在 Three.js 里复刻「改一个字段、多个视图跟着变」，需要自己写订阅层。

## 2.7 逐点结论

| 对比点 | HT | Three.js |
|---|---|---|
| 数据与显示 | Node 同时是业务数据和图元 | Object3D 是渲染对象，业务数据另挂 |
| 关系模型 | DataModel 索引 + Data 父子关系 | Object3D parent/children |
| 属性变更 | s()/a() 广播，视图自动失效 | 普通属性，应用自己同步 |
| 2D/3D 复用 | 同一 DataModel | 需要自行建立映射 |
| 工程序列化 | 属性、样式、数据数组和资源 | 渲染对象、几何和材质 |
| 选择 | SelectionModel 是数据模型的一部分 | 没有内建业务选择模型 |

