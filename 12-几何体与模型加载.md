# 12 几何体与模型加载

> 对比对象：HT 的几何与模型加载，对照 Three.js r125。

## 12.1 两种建模入口

HT 的几何由 shape3d 样式和模型注册表驱动。Node 写入 shape3d 名称后，PrefabRenderer/Shape3dUI 根据样式生成或加载 Shape3dModel。

Three.js 的几何是 BufferGeometry 实例。应用先 new BoxGeometry/SphereGeometry/ExtrudeGeometry 或加载外部模型，再把 geometry 和 material 交给 Mesh。

## 12.2 HT 内建几何

src/renderers/renderers-util-functions.js 提供 roundRect、star、rect、triangle、rightTriangle、parallelogram、trapezoid、billboard、plane 等轮廓/基础工厂。src/graph3d/graph3d-highlight-prefabs.js 注册 box、sphere、cone、cylinder、torus 等 shape3d 模型。

参数由样式决定：

| 样式 | 作用 |
|---|---|
| shape3d | 几何名称或注册模型 |
| shape3d.side | 分段数 |
| shape3d.smooth | 法线平滑 |
| shape3d.start.angle/sweep.angle | 起止角 |
| shape3d.top.cap/bottom.cap | 封顶/封底 |
| shape3d.top.visible/bottom.visible | 分面可见性 |
| shape3d.polyline.depth | 路径拉伸深度 |
| shape3d.section.* | 截面、旋转和扭转 |

几何模型按面保存 vs、ns、uv、is 以及 top/bottom/from/to 等分面数组，便于分面材质和拾取。

## 12.3 Shape 拉伸和自定义模型

src/geometry/shape-extrude.js、shape-paths.js 和 src/graph3d/shape3d-ui.js 把 2D points/segments 变成 3D 拉伸模型。路径可以控制深度、偏移、半径缩放、截面角度和 twist。

自定义模型通过 ht.Default.setShape3dModel(name, model) 注册。model 可以是已生成的 Shape3dModel、带 modelType/url 的描述对象或 URL 字符串。名称相同的模型会复用缓存。

## 12.4 HT 外部模型加载

src/graph3d/loadFaceModel.js、loadFaceModelByUrl.js 和 src/graph3d/view-face-materials.js 根据 modelType 分派：

| modelType | 来源/处理 |
|---|---|
| bin/htb | HT 二进制模型 |
| fbx | FBX 插件或处理器 |
| gltf/glb | glTF 处理器 |
| obj | OBJ/MTL 插件 |
| json | HT 工程或自定义 JSON 模型 |

src/core/core-bootstrap-defaults.js 保存扩展名和默认处理器配置，加载结果交给 finishFaceModelLoad、材质解析和 Graph3dView。模型加载完成后仍由 Shape3dUI/PrefabRenderer 生成渲染批次。

## 12.5 Three.js 几何体

Three.js r125 的基础几何位于 three-0.125.0/package/src/geometries/：BoxGeometry、SphereGeometry、CylinderGeometry、ConeGeometry、TorusGeometry、ShapeGeometry、ExtrudeGeometry 和 LatheGeometry 等。它们直接创建 BufferGeometry，顶点、法线、UV 和 index 存在 BufferAttribute 中。

three 的分段、角度、半径和 bevel 参数在构造时传入。运行中要改变拓扑，通常重新构造几何或修改 BufferAttribute，并手动更新 boundingSphere/boundingBox。

## 12.6 模型加载和场景挂载

Three.js r125 的 GLTFLoader、OBJLoader、FBXLoader、ColladaLoader 等多在 examples/jsm/loaders。每个 loader 负责自己的格式，返回 Object3D、BufferGeometry、Material 或 AnimationClip。应用自行把返回对象 add 到 Scene，并维护缓存和错误处理。

HT 的注册表方式让工程 JSON 只保存模型名或 URL；Three.js 的显式 loader 方式更开放、更容易接入第三方格式，但业务层需要额外做资源路由、复用和序列化。

## 12.7 序列化

HT 的 src/graph/json-serializer.js 以 DataModel 为中心序列化 id、父子关系、业务属性、样式、模型名和场景配置，可恢复为同样的数据图。

Three.js 的 Object3D.toJSON() 以 Scene/Object3D 为中心导出对象、几何、材质、纹理和变换库；业务属性若不放在 userData 或外部数据库中，不会自动拥有 HT 的工程语义。

## 12.8 对比结论

| 对比点 | HT | Three.js r125 |
|---|---|---|
| 几何入口 | shape3d 样式名/注册模型 | BufferGeometry 实例 |
| 分段修改 | 改样式即可重生成 | 通常重建 geometry |
| 分面材质 | top/bottom/from/to 原生支持 | geometry groups + 多材质 |
| 路径拉伸 | Shape3dUI 内建 | ExtrudeGeometry 显式构造 |
| 模型加载 | 名称注册表 + modelType 分派 | loader 实例各自处理 |
| 资源复用 | HT 缓存和工程引用 | 应用自己维护缓存 |
| 序列化 | 业务 DataModel 工程文件 | 渲染场景 JSON |

